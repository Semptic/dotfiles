set nocompatible              " be iMproved, required
filetype off                  " required

set runtimepath+=~/.vim       " set the runtime path to include .vim

source ~/.vim/Vundle.vim
source ~/.vim/vimrc.vim
